{
  "name": "azure-functions-test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "engines": {
    "node": ""
  },
  "scripts": {
    "build": "esbuild src/index.ts --outdir=dist --bundle --platform=node --target=esnext --external:@azure/functions-core --format=cjs --minify && cp package.json dist",
    "watch": "tsc -w",
    "prestart": "bun run build",
    "start": "func start --verbose",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0-alpha.3"
  },
  "devDependencies": {
    "@types/node": "^14.18.31",
    "azurite": "^3.19.0",
    "esbuild": "^0.15.10",
    "func-cli-nodejs-v4": "^4.0.4764",
    "typescript": "^4.0.0"
  }
}
